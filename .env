# User/group the container runs as, so files written into ./library are owned
# by you on the host (no root/sudo needed). Set these to your own IDs if they
# differ — find them with:  id -u  and  id -g
UID=1000
GID=1000
